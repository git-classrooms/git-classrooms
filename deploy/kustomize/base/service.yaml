# https://kubernetes.io/docs/concepts/services-networking/service/
apiVersion: v1
kind: Service
metadata:
  name: git-classrooms
spec:
  selector:
    app: git-classrooms
  type: ClusterIP
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: http

